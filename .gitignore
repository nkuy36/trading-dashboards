# Discord webhook payloads written by the catalyst scan routines during deploy.
# Transient scratch — the notification is POSTed, the file is not part of the site.
discord_msg.json
discord_payload.json
discord_*.json
